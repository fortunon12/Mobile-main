import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerButton: {
    marginTop: 70,
    width: 300,
    height: 70,
    borderRadius: 30,
    backgroundColor: "#21F69E",
    justifyContent: "center",
    alignItems: "center",
  },

  textButton: {
    fontSize: 16,
    fontWeight: "700",
    color: "#fff",
  },
});
